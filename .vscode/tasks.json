{
  "tasks": [
    {
      "type": "shell",
      "label": "(Debug) Build the kernel and run qemu",
      "command": "make debug",
      "options": {
        "cwd": "${workspaceRoot}",
      }
    }
  ],
  "version": "2.0.0"
}